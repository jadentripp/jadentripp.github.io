extends layouts/default

block content
  section.content-page.reveal
    header.page-header
      p.kicker 404
      h1 That page drifted away.
      p.hero-copy The link may have changed, or the page no longer exists.
      p.label-tag Not Found
    p
      a(href=u('/')) Return home
